/**
 * External navigation links used across the app.
 * Centralised here so they can be updated in one place.
 */

export const EXTERNAL_LINKS = {
  /** Project documentation site (set when one exists) */
  documentation: '',
  /** GitHub repository (set when one exists) */
  github: '',
} as const;
